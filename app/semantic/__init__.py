"""Dense semantic search POC (text-embedding model + knn_vector + `neural` query).

Sits alongside the neural-sparse POC on the same Flickr dataset — only the technique
changes. Pure dense semantic (no lexical/hybrid combination) to mirror the OpenSearch
"Semantic search" tutorial.
"""
